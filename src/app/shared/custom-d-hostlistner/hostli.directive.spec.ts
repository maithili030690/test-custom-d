import { HostliDirective } from './hostli.directive';

describe('HostliDirective', () => {
  it('should create an instance', () => {
    const directive = new HostliDirective();
    expect(directive).toBeTruthy();
  });
});
