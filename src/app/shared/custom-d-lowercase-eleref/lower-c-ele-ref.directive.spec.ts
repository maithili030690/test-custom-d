import { LowerCEleRefDirective } from './lower-c-ele-ref.directive';

describe('LowerCEleRefDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerCEleRefDirective();
    expect(directive).toBeTruthy();
  });
});
