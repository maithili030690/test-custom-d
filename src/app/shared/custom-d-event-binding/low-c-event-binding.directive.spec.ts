import { LowCEventBindingDirective } from './low-c-event-binding.directive';

describe('LowCEventBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new LowCEventBindingDirective();
    expect(directive).toBeTruthy();
  });
});
