import { DWithRendererDirective } from './d-with-renderer.directive';

describe('DWithRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new DWithRendererDirective();
    expect(directive).toBeTruthy();
  });
});
