import { PropertyBindingDirective } from './property-binding.directive';

describe('PropertyBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyBindingDirective();
    expect(directive).toBeTruthy();
  });
});
