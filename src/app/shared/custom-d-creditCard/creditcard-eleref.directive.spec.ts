import { CreditcardElerefDirective } from './creditcard-eleref.directive';

describe('CreditcardElerefDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcardElerefDirective();
    expect(directive).toBeTruthy();
  });
});
