import { DWoRendererDirective } from './d-wo-renderer.directive';

describe('DWoRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new DWoRendererDirective();
    expect(directive).toBeTruthy();
  });
});
